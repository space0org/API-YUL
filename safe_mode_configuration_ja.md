# セーフモード設定オプション

Bitcoin SVノードのセーフモード設定に関するドキュメントです。

## セーフモードとは

セーフモードは、Bitcoin SVノードがネットワークにフォークや不整合を検出した場合に、自動的に有効になる保護機能です。セーフモードが有効になると、特定の操作（残高取得など）が制限される場合があります。

## セーフモード警告を無効にする方法

セーフモード警告を無効にするには、`bitcoin.conf`ファイルに以下の設定を追加します：

```
nosafemodewarning=1
```

ただし、これは警告を無効にするだけで、セーフモード自体を無効にするわけではありません。

## セーフモードを完全に無効にする方法

セーフモードを完全に無効にするには、`bitcoin.conf`ファイルに以下の設定を追加します：

```
disablesafemode=1
```

**注意**: セーフモードを完全に無効にすることは、潜在的なネットワークの問題を見逃す可能性があるため、本番環境では推奨されません。

## 注意事項

セーフモード警告や機能を無効にすることは**推奨されません**。セーフモードは、ネットワークにフォークが検出された場合など、潜在的な問題がある場合にノードを保護するための重要な機能です。

## 推奨される対応方法

セーフモード警告が表示された場合は、以下の対応を検討してください：

1. 101ブロック以上のマイニングを行う（テストネットワークの場合）
2. ネットワークが安定するまで待つ
3. ノードを再起動する
4. ブロックチェーンを再同期する

## APIでのセーフモード対応

API-YULでは、セーフモードエラーが発生した場合でも残高を取得できるように修正されています。セーフモード時には以下のレスポンスが返されます：

```json
{
  "address": "mxyz123...",
  "balance": 0.0,
  "unspentOutputs": [],
  "network": "jpy",
  "safeMode": true,
  "safeModeWarning": "Safe mode: Warning: The network does not appear to fully agree! Some miners appear to be experiencing issues. A large valid fork has been detected."
}
```

クライアントアプリケーションでは、`safeMode`フラグをチェックして、適切なエラーメッセージをユーザーに表示することをお勧めします。

## セーフモードとマイニング報酬

セーフモード時でも、マイニング報酬は正常に生成されます。ただし、セーフモードが有効な間は、残高が0として表示される場合があります。101ブロック以上のマイニングを行うことで、セーフモードが解除され、正しい残高が表示されるようになります。

## Docker環境でのセーフモード設定

Docker環境でセーフモード設定を変更するには、以下の手順を実行します：

1. コンテナ内の`bitcoin.conf`ファイルを編集します：

```bash
docker exec -it jpynetwork-node bash -c "echo 'nosafemodewarning=1' >> /root/.bitcoin/bitcoin.conf"
```

2. ノードを再起動します：

```bash
docker restart jpynetwork-node
```

同様の手順でLariNetworkノードも設定できます。
