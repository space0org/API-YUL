# Mainnetモード切り替え機能の調査結果

## 現在の実装状況

現在のAPI-YULでは、ネットワークモードの切り替え機能が部分的に実装されています。具体的には：

1. `/api/network/mode`エンドポイントが実装されており、'regtest'モードと'testnet'モードの切り替えをリクエストすることができます。
2. 'mainnet'モードへの切り替えは現在サポートされていません。

## 技術的な制限

Bitcoin SVノードのネットワークモードを完全に切り替えるには、以下の操作が必要です：

1. Bitcoin SVノードの設定ファイル（bitcoin.conf）の編集
2. ノードの再起動
3. 新しいネットワークとの同期

これらの操作はAPI経由では完全に自動化できないため、実際のモード切り替えには手動での設定変更が必要です。

## 実装した改善点

1. `bitcoin_rpc.py`ファイルを修正し、'mainnet'モードをサポートするようにしました。
2. モード切り替え時に適切な警告メッセージを表示するようにしました。
3. 詳細なドキュメント（mainnet_switching_guide_ja.md）を作成しました。

## 推奨される使用方法

1. 開発・テスト目的では、Regtestモードの使用を推奨します。
2. Mainnetモードへの切り替えが必要な場合は、ドキュメントに記載されている手動での切り替え手順に従ってください。
3. Mainnetモードでは実際の価値を持つBitcoin SVが取引されるため、十分な注意が必要です。

## APIの使用例

### Mainnetモードへの切り替えをリクエスト

```bash
curl -X POST "http://localhost:5002/api/network/mode" \
  -H "Content-Type: application/json" \
  -d '{"mode":"mainnet","network":"jpy"}'
```

このリクエストは警告メッセージを返し、完全な切り替えには手動での設定変更が必要であることを通知します。

### 現在のネットワーク情報の取得

```bash
curl "http://localhost:5002/api/node/info?network=jpy"
```

このリクエストは現在のネットワークモードを含むノード情報を返します。
